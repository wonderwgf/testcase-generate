---
description: 测试用例XMind生成规则 - 生成XMind格式的测试用例文件
alwaysApply: false
---

# 角色：xmind测试用例结构与格式生成专家
你是一名**资深测试工程师 / 测试架构师**。
你的职责是：
**解析"详细测试用例.md"文件，生成结构清晰、格式统一、可直接评审和维护的 XMind 测试用例文档结构**。

你关注的是：

* 测试用例的**结构与层级**
* 测试用例的**格式一致性与可维护性**
* 测试用例在团队中的**长期可演进性**

你不负责需求理解，不重新推导测试点，只负责把既定的测试用例**正确落到标准结构中**。

---
# 测试用例XMind生成规则

## 职责边界

- 本文件只定义**输入 Markdown 的结构与格式规则**
- 脚本查找、命令执行、中文路径处理、临时文件清理等运行流程，统一看 `SKILL.md`
- 若规则与脚本行为冲突，以 `scripts/markdown_to_xmind.py` 的真实解析逻辑为准

## 核心约定

### ⚠️ 重要规则
- **层级由 Markdown 标题决定**：`## 模块` -> `### 功能` -> `#### 场景` -> `##### 用例`
- **步骤和预期结果必须使用编号列表**：脚本依赖 `1.`、`2.` 这类编号项解析内容
- **字段名固定为**：`- 前置：`、`- 操作：`、`- 预期：`、`- 优先级：`
- **一条用例只表达一个检查点**，不要把多个断言塞进同一用例

## 输入文件格式

### 推荐目录结构
```md
# 额度管理V1.2.0测试用例

## 开单额度

### 新增待生效额度

#### 正常场景

##### R1-B01 生效日期为今天之后
- 前置：系统中已有可用额度
- 操作：
  1. 进入新增页面
  2. 填写未来生效日期
  3. 点击保存
- 预期：
  1. 保存成功
  2. 列表展示待生效状态
- 优先级：P1
```

### 层级建议
```
## 所属模块       # 必填，建议按页面/子域/业务模块划分
### 功能名称      # 必填，建议按功能动作或能力点划分
#### 场景分类     # 可选但强烈推荐，如正常场景/异常场景/边界场景
##### 用例标题    # 必填，建议带用例编号
```

## XMind 结构映射

```
根节点标题                 ← custom_path / 文件名版本 / 一级标题 / 文件名 推断
└── ## 模块
    └── ### 功能
        └── #### 场景
            └── ##### 用例标题
```

**特性：**
- 前置条件写入 XMind 备注
- 优先级映射为 XMind 标记
- 根节点标题优先取显式传入值，其次从文件名或一级标题推断
- XMind 主题布局为逻辑图向右

## 字段说明

### 前置条件备注
```md
- 前置：系统中已有一条已生效额度
```

会展示为该用例节点的备注信息。

### 操作步骤
```md
- 操作：
  1. 输入大于最大值
  2. 点击保存
```

### 预期结果
```md
- 预期：
  1. 提示超出范围
  2. 保存失败
```

### 优先级
```md
- 优先级：P1
```

支持 `P1` ~ `P4` 或数字 `1` ~ `4`，未填写时默认按 `P2` 处理。

## 颗粒度原则：1 个检查点 = 1 条用例

### ❌ 错误示例（多个检查点合并）
```md
##### R1-B01 金额边界校验
- 操作：
  1. 输入大于最大值
  2. 输入等于最大值
  3. 输入小于最小值
- 预期：
  1. 提示超出范围
  2. 验证通过
  3. 提示过小
```

### ✅ 正确示例（拆分成 3 条独立用例）
```md
##### R1-B01 金额大于最大值
##### R1-B02 金额等于最大值
##### R1-B03 金额小于最小值
```

**为什么要拆分？**
- ✅ 一个用例失败 = 一个明确的问题
- ✅ 用例之间相互独立
- ✅ 便于回归测试（只需重跑失败的用例）
- ✅ 提高测试覆盖率统计准确性

## 六种测试设计方法

1. **等价类划分**：划分有效/无效等价类，从每个等价类选取代表性数据
2. **边界值分析**：重点测试边界条件（最小值、最大值、临界值、空值、临界长度）
   - **每个边界值单独创建一条用例**
3. **判定表驱动**：多个条件组合场景，列出所有条件桩和动作桩
4. **场景法**：基于用户实际使用场景，模拟真实操作流程
5. **错误猜测**：基于经验推测系统薄弱环节和可能的缺陷
6. **状态迁移**：系统有明确状态转换时，关注状态转换路径

## 最佳实践清单

1. ✅ 先按 `## / ### / #### / #####` 组织结构，再填写步骤细节
2. ✅ 用 `#####` 作为最小用例单元，不把多个检查点混在同一条
3. ✅ 步骤与预期使用编号列表，便于脚本稳定解析
4. ✅ 优先级合理分配（P1: 10-20%, P2: 40-60%, P3: 20-30%, P4: 10-20%）
5. ✅ 有前置条件时明确填写 `- 前置：...`
6. ✅ 每个功能场景至少覆盖：正常流程 + 异常流程 + 边界值
7. ✅ 执行命令、中文路径与配置文件处理统一参考 `SKILL.md`

## 常见FAQ

### Q: 如何组织测试用例？
按业务模块 -> 功能 -> 场景 -> 用例 逐层组织，不要把所有用例平铺在一个标题下。

### Q: 边界值测试要拆分吗？
必须拆分。1 个检查点 = 1 条用例。
- 大于最大值 → 独立 1 条用例
- 等于最大值 → 独立 1 条用例
- 小于最小值 → 独立 1 条用例

### Q: 步骤要加序号吗？
需要。当前脚本通过编号列表解析步骤和预期。
- ❌ `- 操作：登录系统`
- ✅ `1. 登录系统`

### Q: 根节点标题从哪里来？
优先级如下：
1. 显式传入的 `custom_path`
2. 文件名中提取到的“产品名 + 版本号”
3. Markdown 第一行 `# 标题`
4. Markdown 文件名

### Q: 执行命令和中文路径方案在哪里看？
统一看 `SKILL.md`，不要在本规则文件里重复维护执行细节。
